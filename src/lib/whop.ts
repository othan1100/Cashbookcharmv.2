// Paste your Whop checkout links here (one per paid plan).
// Get these from your Whop dashboard → Products → Checkout link.
export const WHOP_LINKS: Record<"pro" | "team", string> = {
  pro: "https://whop.com/checkout/REPLACE_ME_PRO",
  team: "https://whop.com/checkout/REPLACE_ME_TEAM",
};
